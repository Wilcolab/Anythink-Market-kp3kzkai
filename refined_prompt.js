/*

Create a function that sorts an array of numbers in ascending order.

Requirements:
- Do NOT use JavaScript's built-in sort() method.
- The function must not modify the original array.
- Handle edge cases:
    - Empty array → return []
    - Array with one element → return same array
    - Array with duplicate values → duplicates should remain
    - Array with negative numbers → should sort correctly
- Time complexity should be O(n^2) or better.

Function signature:
function sortArray(arr)
*/

function sortArray(arr) {

}
